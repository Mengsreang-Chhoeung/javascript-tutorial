// using document.getElementsByClassName(name)
var heading = document.getElementsByClassName("heading");

// give a color style to those heading one
for (var i = 0; i < heading.length; i++) {
    heading[i].style.color = "brown";
}
