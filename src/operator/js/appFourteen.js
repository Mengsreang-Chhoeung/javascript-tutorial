var a = 30;
document.write("A= ", a, "<br>");
// assign with operator
if (a < 50 || a > 20) {
    document.write("30 is Smaller than 50 or Bigger than 20");
}
document.write("<br>");
if (a < 50 || a < 20) {
    document.write("30 is Smaller than 50 or Smaller than 20");
}

